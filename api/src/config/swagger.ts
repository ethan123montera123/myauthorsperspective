const swagger = {
  title: "My Author's Perspective API",
  description: "Playground for the basic endpoints of My Author's Perspective",
  version: "0.1",

  endpoint: "docs",
};

export type SwaggerConfig = typeof swagger;
export default swagger;
